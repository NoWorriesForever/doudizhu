# 斗地主联机模拟器 · 部署到 Render（免费 · 7×24 在线）

> ⚠️ **实际已改走 Hugging Face Spaces（真正免信用卡）**，因为 Render 现在要求绑卡。请直接看 **《部署到HuggingFace.md》**，按那里的步骤部署。本文件保留作 Render 参考。

目标：把游戏跑在 Render 的免费云服务器上，**你关机、电脑不在，别人也能随时打开网址一起玩**。

本项目是**纯 Node.js、零第三方依赖**，部署极快，`npm install` 几乎秒过。

> ⚠️ 免费档说明：Render 免费服务在**空闲约 15 分钟后会自动休眠**，下次有人访问时会自动唤醒（冷启动约 20~40 秒，首次打开稍慢，之后就快了）。网址**永远有效**，完全满足「关机也能打开」。若想要「永不休眠」，需升级付费档（约 7 美元/月）。

---

## 你只需要做两件事
1. 在自己电脑上把代码推到 GitHub（3 条命令）。
2. 在 Render 网页上点几下，关联 GitHub 仓库并部署。

下面分步来。

---

## 第 1 步：在 GitHub 新建一个空仓库
1. 打开 https://github.com ，登录（没有就注册一个，免费）。
2. 右上角 **「+」→ New repository**。
3. Repository name 填：`doudizhu`
4. 其余**全部保持默认**，**不要**勾选 Add a README / .gitignore / License（仓库必须是空的）。
5. 点 **Create repository**。
6. 建好后页面会显示一个地址，类似：
   ```
   https://github.com/你的用户名/doudizhu.git
   ```
   复制它（下面第 3 步要用）。

---

## 第 2 步：在本机把代码推到 GitHub
> 用 **Git Bash**（不是 PowerShell，避免执行策略问题）。如果没装 Git，先装：https://git-scm.com/download/win

打开 Git Bash，粘贴执行下面几条命令（把 `你的用户名` 换成你真实的 GitHub 用户名）：

```bash
# 进入项目目录
cd "/c/Users/章泓/WorkBuddy/2026-07-07-10-52-08/doudizhu"

# 关联远程仓库（替换成你第 1 步复制的地址）
git remote add origin https://github.com/你的用户名/doudizhu.git

# 推送到 GitHub（首次会弹窗让你登录 GitHub，按提示授权即可）
git push -u origin main
```

看到 `main -> main` 类似输出就成功了。去 GitHub 刷新一下，应该能看到全部代码。

> 如果提示 `git` 不是命令，说明 Git 没装好；重开 Git Bash 再试。
> 如果 push 时弹出登录框，用浏览器登录你的 GitHub 账号授权即可。

---

## 第 3 步：在 Render 一键部署
1. 打开 https://render.com ，点 **Sign Up** 用 **GitHub 账号**登录（免费，无需信用卡）。
2. 登录后点右上角 **New → Blueprint**。
3. 关联你的 GitHub 账号，在仓库列表里选择 **`doudizhu`**。
4. Render 会自动读取仓库里的 `render.yaml`，显示要创建的服务 `doudizhu`（plan: free）。
5. 点 **Apply / Create New Resources**，开始部署。
6. 等待 1~2 分钟，状态变绿（Live）即可。

部署完成后，Render 会给你一个**永久网址**，形如：
```
https://doudizhu.onrender.com
```
把这个网址发给你朋友，他们随时打开就能和你一起玩，无需你开机。

---

## 第 4 步：验证
- 打开 `https://doudizhu.onrender.com`，能进大厅、建房间、加机器人、出牌 = 成功。
- 如果显示「Application error」：等几十秒再刷新（免费服务刚唤醒）；若持续报错，回 Render 的 Logs 页看报错信息发给我。

---

## 常见问题
**Q：免费档休眠了，朋友打不开？**
A：第一次访问会冷启动约 30 秒，稍等刷新即可。之后有人玩就不会睡。

**Q：想要自己的专属域名（如 ddz.我的域名.com）？**
A：Render 免费档支持自定义子域，在服务的 Settings → Custom Domains 里按提示添加并配 DNS 即可。

**Q：以后我改了代码想更新到线上？**
A：在本机 Git Bash 里改完执行：
```bash
cd "/c/Users/章泓/WorkBuddy/2026-07-07-10-52-08/doudizhu"
git add -A
git commit -m "更新"
git push
```
Render 会自动重新部署（Auto-Deploy 默认开启）。

**Q：想彻底不休眠？**
A：Render 付费档（约 $7/月）或 Railway/Fly.io（需绑卡）。需要的话告诉我，我帮你改配置。
