# 斗地主联机模拟器 · 部署到 Render（免费档 · 7×24 在线）

目标：把游戏跑在 Render 的免费云服务器上，**你关机、电脑不在，别人也能随时打开网址一起玩**。

本项目是**纯 Node.js、零第三方依赖**，部署极快（`npm install` 几乎秒过），`render.yaml` 已配置好用 Render 原生 Node 运行时。

> ⚠️ **关于绑卡（必读）**：Render 的免费档现在**要求绑定信用卡**作身份验证。但只要你保持 **plan: free** 不手动升级、不超出免费额度，就**不会扣费**（免费额度含每月 750 小时 Web 服务，单台服务 24/7 跑也够用）。绑卡只是防滥用验证，不是要收费。
>
> ⚠️ **免费档休眠**：空闲约 15 分钟后服务会自动休眠，下次有人访问时自动唤醒（冷启动约 20~40 秒，首次打开稍慢）。**网址永久有效**，完全满足「关机也能打开」。

---

## 你已完成的前提（确认）
- GitHub 仓库 `https://github.com/NoWorriesForever/doudizhu` 已建好，代码已 push（含 `render.yaml`）。
- 若想同步我后来在本地改过的文件（Dockerfile / 文档），可在 Git Bash 里先 `git push origin main`；但 **GitHub 上现有代码已可直接部署**，这步可选。

---

## 第 1 步：用无痕窗口登录 Render（避开插件拦截）
你之前遇到过 `We couldn't complete sign-in`（插件拦截授权跳转），用无痕窗口即可解决：

1. 按 **Ctrl + Shift + N**（Chrome）或 **Ctrl + Shift + P**（Edge）开**无痕 / InPrivate 窗口**。
2. 打开 **https://dashboard.render.com/** 。
3. 点 **Sign Up**（或 Log In）→ 选 **「Continue with GitHub」**。
4. 弹出的 GitHub 授权页里：
   - 若右上角是「Sign in」而不是头像，**先登录 GitHub 账号**，登录后「Authorize」按钮才会变可点。
   - 点 **Authorize Render**（或 Authorize git-ecosystem）→ 授权成功回到 Render。
   > 无痕窗口默认不加载广告/隐私插件，这一步基本一次过。若还卡，换浏览器或临时禁用 uBlock/AdBlock 类插件。

---

## 第 2 步：绑定信用卡（仅验证，不扣费）
1. 登录后右下角头像 → **Account Settings → Billing**。
2. 点 **Add payment method**，填信用卡信息保存。
3. 不会立即扣费。如果想彻底放心，可在同页把 **Spend Limit / 用量上限** 设低或开启告警邮件（免费档正常用不到上限）。

---

## 第 3 步：一键 Blueprint 部署
1. 回到 Dashboard，点右上角蓝色 **「New +」→ Blueprint**。
2. 首次会请求连接 GitHub：点 **Authorize / Connect**，在仓库权限列表里**手动把 `doudizhu` 勾上并保存**（GitHub 默认可能只授权部分仓库，不勾会找不到仓库）。
3. 在仓库列表选 **`doudizhu`** → Render 自动读取 `render.yaml`，显示要创建的服务：
   - 类型 Web Service，名字 `doudizhu`，**Plan: Free**，Node 22，build `npm install`，start `npm start`。
4. 点 **Apply / Create New Resources** 开始部署。
5. 看 **Build** 日志约 1~2 分钟，状态变 **Live（绿色）** 即成功。

> 若 Blueprint 没自动识别，可手动：New + → **Web Service** → 连 GitHub 选 `doudizhu` → Environment 选 Node → Build Command 填 `npm install`、Start Command 填 `npm start` → 其余默认 → Create。

---

## 第 4 步：拿到永久网址
服务页面会显示一个网址，形如：
```
https://doudizhu.onrender.com
```
把它发给你朋友，他们随时打开就能进大厅和你玩，**无需你开机**。

---

## 第 5 步：验证
- 打开网址，能进大厅、建房间、加机器人、出牌 = 成功。
- 首次打开可能要等 ~30 秒（冷启动唤醒），稍等刷新即可。
- 若持续显示「Application error」：到服务页 **Logs** 看报错，把报错发我。

---

## 常见问题
**Q：真的不会扣费吗？**
A：只要服务是 **Free** 计划、你不手动升级，就不会扣。可在 Billing 设用量上限兜底。

**Q：免费档休眠了，朋友打不开？**
A：第一次访问会冷启动约 30 秒，稍等刷新即可；之后只要有人玩就不会睡。

**Q：想用自己的专属域名（如 ddz.我的域名.com）？**
A：在服务 Settings → Custom Domains 按提示添加并配 DNS（免费档支持）。

**Q：以后改了代码怎么更新到线上？**
A：在本机 Git Bash 改完执行：
```bash
cd "/c/Users/章泓/WorkBuddy/2026-07-07-10-52-08/doudizhu"
git add -A
git commit -m "更新"
git push
```
Render 默认 **Auto-Deploy** 会自动重新部署。

**Q：想彻底不休眠？**
A：升级 Render 付费档（约 $7/月）或换 Railway/Fly.io（需绑卡）。需要的话告诉我。
