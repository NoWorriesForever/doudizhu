# 免费方案：国内可访问 · 朋友各自网络设备游玩

> **适用场景**：你想和朋友在各自的手机/电脑、各自的网络（WiFi/流量）下一起玩斗地主，且不花钱。
>
> **原理**：你的电脑跑斗地主服务（`node node-server.js`），用 **cpolar 免费内网穿透**把本地服务暴露成一个公网网址，朋友用这个网址就能访问——国内直连、手机电脑都行。
>
> **7×24 说明（诚实告知）**：此方案下"电脑开着 + cpolar 运行"时，朋友随时可玩；电脑关机/休眠后服务中断。这是**纯免费**方案的固有限制（见文末「真·7×24 可选升级」）。但斗地主是回合制，玩之前你开一下电脑、把地址发群里，完全够用。

---

## 一、你的电脑准备（只需做一次）

### 1. 确认已装 Node.js
打开 CMD（开始菜单搜 `cmd`），输入：
```
node -v
```
能看到版本号（如 `v20.x` / `v22.x`）即可。没装的话去 https://npmmirror.com/mirrors/node/ 下载 LTS 版安装。

### 2. 安装依赖
在 CMD 里进入项目目录，装一次依赖：
```
cd 你的项目路径\doudizhu
npm install
```
（会安装 `ws` 这个 WebSocket 库，几秒钟完成。）

### 3. 启动服务
```
node node-server.js
```
看到这行就成功了：
```
[斗地主] 服务已启动  http://0.0.0.0:3000  (Node WebSocket 版)
```
现在浏览器打开 **http://localhost:3000** ，能正常进大厅/建房就说明本地服务没问题。

> 这个 CMD 窗口不要关（关了服务就停）。下面用 cpolar 把它暴露到公网。

---

## 二、cpolar 免费内网穿透（把本地服务变成公网网址）

### 1. 注册 cpolar（免费）
打开 https://www.cpolar.com/ ，点注册，填邮箱即可（免费版不需要付费）。

### 2. 下载 cpolar 客户端
登录后在「仪表盘 → 安装」里下载 Windows 版，解压到任意目录（如 `C:\cpolar`）。

### 3. 配置你的认证 token
在 cpolar 网站的「仪表盘 → 验证」里复制你的 Authtoken，然后在 CMD 里执行：
```
cd C:\cpolar
cpolar authtoken 你复制的token
```

### 4. 开隧道，生成公网网址
```
cpolar http 3000
```
执行后会显示类似这样的输出：
```
Forwarding     http://xxxxxx.cpolar.cn -> http://localhost:3000
Forwarding     https://xxxxxx.cpolar.cn -> http://localhost:3000
```
其中 **`xxxxxx.cpolar.cn`** 就是你的公网网址！

### 5. 把网址发给朋友
朋友在手机或电脑浏览器打开 `http://xxxxxx.cpolar.cn`（或 https 版），就能进大厅、建房/加入、一起玩了。

> **重要**：你自己的电脑也要保持 `node node-server.js` 和 `cpolar http 3000` 两个窗口开着。关了就断。

---

## 三、使用流程（每次玩之前）

1. 开 CMD，`cd doudizhu`，`node node-server.js`（留窗口1）
2. 再开一个 CMD，`cd C:\cpolar`，`cpolar http 3000`（留窗口2）
3. 复制窗口2里的 `xxxxxx.cpolar.cn` 网址，发给朋友
4. 大家打开网址，建房/加入，开玩

> **免费版注意**：cpolar 免费版每次启动隧道，域名是随机的（会变）。所以每次玩之前重新开一下隧道、发新地址即可。对"约朋友玩"完全够用。

---

## 四、常见问题

**Q：朋友打开网址白屏 / 连不上？**
- 确认你电脑上 `node node-server.js` 窗口还在、没报错
- 确认 `cpolar http 3000` 窗口还在、显示 `online`
- cpolar 免费版有带宽限制（约 1Mbps），斗地主是纯文本数据，足够；但如果多人同时访问偶尔会慢，刷新即可

**Q：手机上 WebSocket 连不上 / 不实时？**
- 前端已内置轮询兜底（800ms），即使 WS 断了也能玩，只是稍微慢一点
- 确认网址用 `http://` 开头（免费版 cpolar 的 http 隧道，WS 走 ws:// 同站转发）

**Q：电脑休眠了服务断怎么办？**
- 设置电脑「从不休眠」（控制面板 → 电源选项 → 从不睡眠）
- 或者玩之前把电脑设置成不休眠

**Q：可以几个人同时玩？**
- 每个房间 3 人（不够可加机器人）。可以同时开多个房间号，不同房间互不影响。

---

## 五、真·7×24 可选升级（需少量付费，非必须）

上面的免费方案需要你的电脑开着。如果你想要**关机也能 7×24 在线**，有两个升级路径：

### 方案 A：一台旧电脑 / 迷你主机长期开着（零月费）
把 `doudizhu` 文件夹拷到一台不用的旧电脑/迷你主机（树莓派也行），装好 Node，设置开机自动运行 `node node-server.js` + `cpolar http 3000`，常开不关。零月费，只是费一点电。

### 方案 B：国内轻量应用服务器（约 ¥60–100/年）
腾讯云/阿里云的轻量应用服务器，买最便宜套餐（国内节点），把代码传上去跑。**纯公网 IP + 端口访问，不需要备案域名**（备案只管域名，纯 `IP:端口` 不强制备案）。
- 服务器装 Node，传 `doudizhu/` 目录上去
- `npm install && node node-server.js`（用 pm2 守护进程保活）
- 安全组放通 3000 端口
- 朋友用 `http://服务器公网IP:3000` 访问

> 这两个方案代码完全不用改——`node-server.js` 已经是平台无关的，哪台机器有 Node 就能跑。

---

## 附：为什么不用 Cloudflare？

之前部署在 Cloudflare Workers（`*.workers.dev`），但该子域在国内被墙/极慢，手机基本打不开；而让 Cloudflare 在国内通需要「已备案域名 + 企业版」（约 $200/月），不划算。所以改成纯 Node 服务 + cpolar 穿透，国内直连无障碍。

代码三套并存，互不影响：
- `node-server.js` —— 纯 Node WS 版（**国内免费方案用这个**）
- `server.js` —— 旧 SSE 版（兼容参考）
- `worker.js` + `room-do.js` —— Cloudflare 版（海外可用）
